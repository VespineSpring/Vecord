from setuptools import setup, find_packages

setup(
    name="vecord",
    version="0.0.1",
    author="Vespine Spring",
    description="A discord library so I do not need to write the same things again and again.",
    url="https://github.com/VespineSpring/Vecord",
    packages=find_packages()
)
